# ===== 대용량 바이너리 (GitHub 100MB 제한 초과) =====
*.tar
*.zip
msa-lecture-images.part.*

# ===== 강의 교재 / 가이드 PDF (제3자 저작물) =====
*.pdf

# ===== 원본 실습 템플릿 (참고용 로컬 사본, 저장소에는 포함하지 않음) =====
msa-lecture/

# ===== 의존성 =====
node_modules/

# ===== 빌드 산출물 =====
build/
bin/
dist/
out/
.gradle/
*.class
*.jar
# 하위 서비스의 래퍼까지 포함되도록 **/ 를 붙인다.
# (경로 구분자가 들어간 패턴은 .gitignore 위치 기준으로 고정된다)
!**/gradle/wrapper/gradle-wrapper.jar

# ===== IDE / OS =====
.DS_Store
.idea/
.vscode/
*.iml
*.log

# 로컬 전용 compose 오버라이드
docker-compose.override.yml

# 시크릿 (API 키 등). .env.example 만 커밋한다.
.env
.env.local
!.env.example

# 예외: 프론트엔드 dev 설정. VITE_ 접두사 값은 어차피 번들에 그대로 들어가므로
# 비밀이 아니고, 팀원이 클론 후 바로 실행하려면 저장소에 있어야 한다.
!msa-MyService/vue-frontend/.env

# 실수로 중첩 클론된 저장소 (있으면 무시)
/Agile-MSA/
